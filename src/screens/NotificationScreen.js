import React from "react";

import {
    SafeAreaView,
    ScrollView,
    StatusBar,
    StyleSheet,
    Text,
    useColorScheme,
    View,
    TextInput,
    TouchableOpacity,
  } from 'react-native';

  import PrimaryButton from '../components/PrimaryButton';

  function NotificationScreen() {
    return(
        <SafeAreaView>
            <Text style={{fontSize:24, fontWeight:'bold'}}>Under Development</Text>
        </SafeAreaView>
    );
  }

  export default NotificationScreen;